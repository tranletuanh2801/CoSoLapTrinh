#index(): trả về index của phần tử trong list
#vd: 
# i=0
# if x in L:
#     while i<len(L): bằng với del(L[L.index(x)])
#         if L[i]==x:
#             def(L[i])
#         i=i+1

#append(x): thêm phần tử x vào cuối L
#insert(i,x): chèn x vào vị trí i hiện có trong L

#del(L[i]): phải biết số index i
#L.remove(x): xoá phần tử x đầu tiên tìm thấy 
#nếu muốn xoá hết while x in L
#L.clear(): xoá tất cả phần tử  
#L.pop(i): xoá và lấy ra gtri của phần tử có chỉ mục i 
#nếu i để tróng thì lấy cuối  

#sort(): cho phép sắp xếp các phần tử trong L có thứ tự
#sort(reverse=True): xếp giảm dần
#reverse(): thực hiện đảo thứ tự các phần tử

#count(x): đếm số phần tử x xuất hiện 
#copy():thực hiện bản sao